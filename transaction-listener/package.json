{
  "name": "transaction-listener",
  "version": "0.0.1",
  "description": "Listens to Ripple transactions on a WebSocket server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^3.8.20",
    "ws": "^0.4.32"
  }
}
