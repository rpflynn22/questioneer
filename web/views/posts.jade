extends layout

block content
  .container
    h1 Questioneer Posts
    br
    table.table.table-striped.table-condensed
      thead
        tr
          th 
          th Title
          th User
          th Bounty
          th Open?
          th Date

      tbody
        each post in posts
          tr
            td
              button(href="/posts/" + post._id, class="btn btn-info") View
            td
              = post.title
            td
              = post.user.username
            td
              = post.bounty
            td
              = post.answered ? "No" : "Yes"
            td
              = post.date.toLocaleString().split(' ').slice(1, 5).join(' ')
